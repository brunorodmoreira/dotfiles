# Login-shell setup.
# All PATH and environment setup has moved to .zshenv so it applies uniformly
# across all shell types (login, interactive, non-interactive). Keeping this
# file empty avoids duplicate initialization and divergent environments.
